import 'package:flutter/material.dart';

class IOSBottomBar5Tabs extends StatelessWidget {
  const IOSBottomBar5Tabs({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const IOSBottomBar5Tabs();
  }
}
